# 初级前端三天复习计划 - 第2天

> 📅 **学习时间**：第2天 | **目标**：HTML+CSS进阶 + JS进阶 + HTTP网络 + Vue进阶 + 性能优化进阶 | **建议时长**：8小时

---

## 📚 今日学习内容导航

| 序号 | 模块 | 核心知识点 | 重要程度 | 预计时长 | 文件链接 |
|------|------|-----------|---------|----------|----------|
| 1 | **HTML和CSS进阶** | BFC、层叠上下文、水平垂直居中、响应式布局、浮动清除 | ⭐⭐⭐⭐⭐ | 1.5小时 | [HTML面试总结大全.md](../面试总结大全/HTML面试总结大全.md)<br>[CSS高频面试题汇总.md](./CSS高频面试题汇总.md) |
| 2 | **JavaScript进阶** | 事件循环、Promise、async/await、宏微任务、模块化 | ⭐⭐⭐⭐⭐ | 2.5小时 | [JS高频面试题汇总.md](./JS高频面试题汇总.md)<br>[事件循环面试题详解.md](./事件循环面试题详解.md) |
| 3 | **HTTP网络专项** | HTTP/HTTPS、GET/POST、状态码、缓存机制、跨域、AJAX | ⭐⭐⭐⭐⭐ | 2小时 | [HTTP面试题精简版.md](./HTTP面试题精简版.md)<br>[协商缓存和跨域面试题.md](./协商缓存和跨域面试题.md)<br>[AJAX经典面试题.md](./AJAX经典面试题.md) |
| 4 | **Vue进阶** | 生命周期、路由模式、路由守卫、Vuex/Pinia | ⭐⭐⭐⭐⭐ | 1.5小时 | [Vue面试总结大全.md](../面试总结大全/Vue面试总结大全.md)<br>[Vue生命周期面试题大全.md](./Vue生命周期面试题大全.md)<br>[Vuex和Pinia面试题精简版.md](./Vuex和Pinia面试题精简版.md) |
| 5 | **性能优化进阶** | 图片优化、懒加载、代码分割、Webpack优化 | ⭐⭐⭐⭐⭐ | 0.5小时 | [前端性能优化面试题精简版.md](./前端性能优化面试题精简版.md) |

---

## 📖 详细学习路径

### 第一模块：HTML和CSS进阶（1.5小时）

#### ✅ HTML进阶（0.5小时）→ [HTML面试总结大全.md](../面试总结大全/HTML面试总结大全.md)

1. **SEO优化核心技巧**
   - title标签：每页唯一，50-60字符，包含关键词
   - meta description：155-160字符，吸引点击
   - h1-h6标签：h1每页一个，体现页面主题
   - alt属性：描述图片内容，提升图片搜索排名
   - 语义化HTML：正确使用标签，增强SEO效果

2. **常用meta标签**
   ```html
   <!-- 字符编码 -->
   <meta charset="UTF-8">

   <!-- 视口设置（移动端） -->
   <meta name="viewport" content="width=device-width, initial-scale=1.0">

   <!-- SEO相关 -->
   <meta name="description" content="页面描述">
   <meta name="keywords" content="关键词1, 关键词2">
   ```

#### ✅ CSS进阶（1小时）→ [CSS高频面试题汇总.md](./CSS高频面试题汇总.md)

1. **BFC（块级格式化上下文）** → [CSS高频面试题汇总.md](./CSS高频面试题汇总.md#2-bfc块级格式化上下文)
   - BFC的定义：独立的渲染区域，内部元素不影响外部
   - 如何创建BFC：
     - overflow: hidden/auto/scroll
     - float: left/right
     - position: absolute/fixed
     - display: inline-block/flex/grid
   - BFC的作用：
     - 清除浮动
     - 防止margin重叠
     - 自适应两栏布局

2. **层叠上下文（Stacking Context）** → [CSS高频面试题汇总.md](./CSS高频面试题汇总.md#11-层叠上下文)
   - z-index的工作原理：只在定位元素中有效
   - 层叠上下文的创建条件：
     - position + z-index（非auto）
     - opacity < 1
     - transform、filter等
   - 层叠顺序：background < 负z-index < 块级盒子 < 浮动 < 内联(inline/inline-block,含文本) < z-index:0 < 正z-index

3. **水平垂直居中方案** → [CSS高频面试题汇总.md](./CSS高频面试题汇总.md#6-水平垂直居中)
   - Flex布局：`display: flex; justify-content: center; align-items: center`
   - Grid布局：`display: grid; place-items: center`
   - 绝对定位 + transform：`position: absolute; top: 50%; left: 50%; transform: translate(-50%, -50%)`
   - 绝对定位 + margin auto：`position: absolute; top: 0; left: 0; right: 0; bottom: 0; margin: auto`

4. **响应式布局与多端自适配** → [CSS高频面试题汇总.md](./CSS高频面试题汇总.md#9-响应式布局)

   **响应式布局（核心方案）：**
   - 媒体查询（Media Query）：`@media (max-width: 768px) { ... }`
   - 移动优先 vs 桌面优先
   - 常见断点：320px、768px、1024px、1200px
   - 单位选择：rem、em、vw/vh、%

   **多端自适配（更广概念）：**
   - 定义：一套代码适配多端（PC、平板、手机、小程序、App等）
   - 与响应式关系：响应式布局是实现多端自适配的主流方案
   - 核心技术：响应式布局 + rem/vw + Flex/Grid + 媒体查询

5. **浮动和清除浮动** → [CSS高频面试题汇总.md](./CSS高频面试题汇总.md#12-浮动和清除浮动)
   - float的工作原理：脱离文档流，但不脱离文本流
   - 浮动导致的问题：父元素高度塌陷
   - 清除浮动的方法：
     - clearfix伪元素
     - overflow: hidden（创建BFC）
     - 父元素也浮动

---

### 第二模块：JavaScript进阶（2.5小时）

#### ✅ 必学知识点

1. **事件循环（Event Loop）** → [JS高频面试题汇总.md](./JS高频面试题汇总.md#18-事件循环) + [事件循环面试题详解.md](./事件循环面试题详解.md)

   **执行机制：**
   - 调用栈（Call Stack）：执行同步代码
   - 宏任务队列（Macro Task）：存放宏任务回调（setTimeout、setInterval、I/O）
   - 微任务队列（Micro Task）：存放微任务回调（Promise.then、MutationObserver、queueMicrotask）

   **核心概念：**
   - 宏任务和微任务本质都是**异步回调函数**
   - 区别在于回调执行的时机和优先级

   **执行顺序（完整版）：**
   1. 执行同步代码（清空调用栈）
   2. 执行微任务队列中的所有回调（清空微任务队列）
   3. 从宏任务队列取出一个任务，执行其回调函数
   4. 执行步骤3产生的所有微任务回调（清空微任务队列）
   5. 浏览器渲染（可选，不是每轮都渲染）
   6. 重复步骤3-5

   **简化记忆：**
   - 同步代码 → 微任务回调（全清） → 宏任务回调（一个） → 微任务回调（全清） → 渲染 → 循环

   **典型例题：**
   ```javascript
   console.log(1);
   setTimeout(() => console.log(2), 0);
   Promise.resolve().then(() => console.log(3));
   console.log(4);
   // 输出：1 4 3 2
   ```

2. **Promise深入理解** → [JS高频面试题汇总.md](./JS高频面试题汇总.md#20-promise-asyncawait)

   **三种状态：**
   - pending（进行中）
   - fulfilled（已成功）
   - rejected（已失败）

   **状态转换：**
   - pending → fulfilled（不可逆）
   - pending → rejected（不可逆）

   **链式调用：**
   - then：处理成功和失败
   - catch：捕获错误
   - finally：无论成功失败都执行

   **静态方法：**
   ```javascript
   // Promise.all - 全部成功才成功
   Promise.all([p1, p2, p3]).then(results => {});

   // Promise.race - 第一个完成就返回
   Promise.race([p1, p2, p3]).then(result => {});

   // Promise.allSettled - 等待全部完成
   Promise.allSettled([p1, p2, p3]).then(results => {});

   // Promise.any - 第一个成功就返回
   Promise.any([p1, p2, p3]).then(result => {});

   // ✨ Promise.withResolvers - ES2024新增，更方便地创建Promise
   const { promise, resolve, reject } = Promise.withResolvers();
   // 等价于传统写法：
   // let resolve, reject;
   // const promise = new Promise((res, rej) => { resolve = res; reject = rej; });

   // ✨ Promise.try - ES2025新增(Stage 4)，统一处理同步/异步函数
   Promise.try(() => getUserData(userId))
     .then(data => console.log(data))
     .catch(err => console.error(err)); // 自动捕获同步和异步错误
   // 等价于传统写法：
   // Promise.resolve().then(() => getUserData(userId))
   ```

   **静态方法对比表：**
   | 方法 | 返回时机 | 结果 | 使用场景 | ES版本 |
   |------|----------|------|----------|---------|
   | **all** | 全部成功或任一失败 | 全部成功的结果数组/第一个失败 | 并发请求，全部成功才继续 | ES2015 |
   | **race** | 第一个完成（成功或失败） | 第一个完成的结果 | 超时控制、多源请求取最快 | ES2015 |
   | **allSettled** | 全部完成（不管成功失败） | 所有结果的状态数组 | 批量操作，需要知道每个结果 | ES2020 |
   | **any** | 第一个成功或全部失败 | 第一个成功的结果/全部失败 | 容错请求、多源备份 | ES2021 |
   | **withResolvers** | - | {promise, resolve, reject} | 需要外部控制Promise状态 | ✨ ES2024 |
   | **try** | - | Promise(执行结果或错误) | 统一同步/异步错误处理 | ✨ ES2025 |

3. **async/await** → [JS高频面试题汇总.md](./JS高频面试题汇总.md#20-promise-asyncawait)

   **基本使用：**
   ```javascript
   async function fetchData() {
     try {
       const response = await fetch('/api/data');
       const data = await response.json();
       return data;
     } catch (error) {
       console.error('Error:', error);
     }
   }
   ```

   **错误处理：**
   - try/catch 捕获
   - .catch() 捕获

   **并行执行：**
   ```javascript
   // 并行执行
   const [result1, result2] = await Promise.all([
     fetch('/api/1'),
     fetch('/api/2')
   ]);

   // 串行执行
   const result1 = await fetch('/api/1');
   const result2 = await fetch('/api/2');
   ```

4. **ES6模块化** → [JS高频面试题汇总.md](./JS高频面试题汇总.md#21-es6-模块化)

   **📌 面试话术（完整版）：**

   **第一步：解释概念**
   > "ES6模块化和CommonJS都是**JavaScript的模块化规范**，用来解决代码拆分和复用的问题。
   >
   > - **ES6模块化**（ES Modules）：ES2015引入的官方标准，使用`import/export`语法，现代浏览器和前端项目的标准方案
   > - **CommonJS**：Node.js采用的模块化规范，使用`require/module.exports`语法，主要用于服务端"

   **第二步：说明核心区别**
   > "两者主要有三点区别：
   > 1. **加载时机不同**：ES6是编译时静态加载，代码执行前就确定依赖关系；CommonJS是运行时动态加载，执行到require才加载
   > 2. **输出机制不同**：ES6输出的是值的引用，模块内部变化会影响外部；CommonJS输出的是值的拷贝，一旦输出就不会变
   > 3. **优势不同**：ES6因为静态分析，支持Tree Shaking优化，可以去除未使用代码，打包体积更小；CommonJS不支持"

   **第三步：实际应用**
   > "实际开发中，前端项目统一用ES6模块化，配合Webpack等打包工具；Node.js项目传统用CommonJS，但现在也支持ES6模块化了。"

   **ES Modules：**
   ```javascript
   // 导出
   export const name = 'Vue';
   export function sayHi() {}
   export default class App {}

   // 导入
   import App, { name, sayHi } from './app.js';
   import * as utils from './utils.js';
   ```

   **CommonJS：**
   ```javascript
   // 导出
   module.exports = { name, sayHi };
   exports.name = 'Vue';

   // 导入
   const { name, sayHi } = require('./app.js');
   ```

   **核心区别对比：**
   | 特性 | ES Modules | CommonJS |
   |------|-----------|----------|
   | **加载方式** | 静态加载（编译时） | 动态加载（运行时） |
   | **输出机制** | 值的引用（动态绑定） | 值的拷贝 |
   | **this指向** | undefined | module |
   | **Tree Shaking** | ✅ 支持 | ❌ 不支持 |
   | **使用场景** | 浏览器、现代前端 | Node.js |

   **记忆口诀：**
   ```
   ES6静态引用支持树摇、CommonJS动态拷贝用于Node
   前端项目首选ES6、服务端Node用CommonJS
   ```

---

### 第三模块：HTTP网络专项（2小时）

#### ✅ 必学知识点

1. **HTTP vs HTTPS** → [HTTP面试题精简版.md](./HTTP面试题精简版.md#1-http-vs-https-)

   **主要区别：**
   - 端口：HTTP 80、HTTPS 443
   - 安全性：HTTPS有SSL/TLS加密，HTTP明文传输
   - 证书：HTTPS需要CA证书
   - 性能：HTTPS握手耗时，但HTTP/2优化了性能

   **HTTPS加密过程：**
   1. 客户端发送Client Hello（支持的加密算法）
   2. 服务器发送Server Hello + 证书
   3. 客户端验证证书
   4. 密钥交换（非对称加密）
   5. 数据传输（对称加密）

2. **GET vs POST** → [HTTP面试题精简版.md](./HTTP面试题精简版.md#2-get-vs-post-)

   | 特性 | GET | POST |
   |------|-----|------|
   | 参数位置 | URL查询字符串 | 请求体 |
   | 数据大小 | 受URL长度限制（2KB） | 理论无限制 |
   | 安全性 | 参数暴露在URL | 相对安全 |
   | 幂等性 | 幂等（多次请求结果相同） | 非幂等 |
   | 缓存 | 可缓存 | 默认不缓存 |
   | 书签 | 可收藏为书签 | 不可收藏 |
   | 使用场景 | 查询数据 | 提交数据、修改数据 |

3. **HTTP状态码** → [HTTP面试题精简版.md](./HTTP面试题精简版.md#4-http-状态码-)

   - **2xx 成功：**
     - 200 OK：请求成功
     - 201 Created：资源创建成功
     - 204 No Content：请求成功但无返回内容

   - **3xx 重定向：**
     - 301 Moved Permanently：永久重定向
     - 302 Found：临时重定向
     - 304 Not Modified：使用缓存

   - **4xx 客户端错误：**
     - 400 Bad Request：请求参数错误
     - 401 Unauthorized：未授权
     - 403 Forbidden：禁止访问
     - 404 Not Found：资源不存在

   - **5xx 服务器错误：**
     - 500 Internal Server Error：服务器内部错误
     - 502 Bad Gateway：网关错误
     - 503 Service Unavailable：服务不可用

4. **HTTP缓存机制** → [协商缓存和跨域面试题.md](./协商缓存和跨域 面试题.md)

   **强缓存：**
   - Cache-Control：max-age=3600、no-cache、no-store
   - Expires：过期时间（HTTP/1.0）
   - 命中强缓存：直接使用缓存，状态码200（from cache）

   **协商缓存：**
   - ETag / If-None-Match：文件内容哈希
   - Last-Modified / If-Modified-Since：文件修改时间
   - 命中协商缓存：返回304 Not Modified

   **缓存流程：**
   1. 浏览器发起请求
   2. 检查强缓存（Cache-Control、Expires）
   3. 未过期：直接使用缓存
   4. 已过期：发送协商缓存请求（带If-None-Match或If-Modified-Since）
   5. 服务器判断资源是否修改
   6. 未修改：返回304，使用缓存
   7. 已修改：返回200和新资源

5. **跨域问题** → [协商缓存和跨域面试题.md](./协商缓存和跨域面试题.md#三跨域问题)

   **同源策略：**
   - 协议、域名、端口三者完全相同

   **跨域解决方案：**

   - **CORS（跨域资源共享）**：
     ```javascript
     // 服务器设置响应头
     Access-Control-Allow-Origin: *
     Access-Control-Allow-Methods: GET, POST, PUT
     Access-Control-Allow-Headers: Content-Type
     Access-Control-Allow-Credentials: true
     ```

   - **JSONP**：
     - 原理：利用script标签不受同源策略限制
     - 缺点：只支持GET请求
     ```javascript
     function jsonp(url, callback) {
       const script = document.createElement('script');
       script.src = `${url}?callback=${callback}`;
       document.body.appendChild(script);
     }
     ```

   - **代理服务器**：
     - webpack-dev-server proxy
     - Vite proxy
     - nginx反向代理
  
6. **AJAX** → [AJAX经典面试题.md](./AJAX经典面试题.md)

   **XMLHttpRequest：**
   ```javascript
   const xhr = new XMLHttpRequest();
   xhr.open('GET', '/api/data');
   xhr.onreadystatechange = function() {
     if (xhr.readyState === 4 && xhr.status === 200) {
       console.log(xhr.responseText);
     }
   };
   xhr.send();
   ```

   **五种状态：**
   - 0 UNSENT：未初始化
   - 1 OPENED：已调用open
   - 2 HEADERS_RECEIVED：已接收响应头
   - 3 LOADING：正在接收响应体
   - 4 DONE：请求完成

   **Fetch API：**
   ```javascript
   fetch('/api/data')
     .then(response => response.json())
     .then(data => console.log(data))
     .catch(error => console.error(error));
   ```

---

### 第四模块：Vue进阶（1.5小时）

#### ✅ 必学知识点

1. **Vue生命周期** → [Vue面试总结大全.md](../面试总结大全/Vue面试总结大全.md#1-vue生命周期) + [Vue生命周期面试题大全.md](./Vue生命周期面试题大全.md)

   **Vue3生命周期钩子：**
   - setup：组合式API入口，最先执行
   - onBeforeMount：挂载前
   - onMounted：挂载后（可访问DOM）
   - onBeforeUpdate：更新前
   - onUpdated：更新后
   - onBeforeUnmount：卸载前
   - onUnmounted：卸载后

   **Vue2生命周期钩子：**
   - beforeCreate、created
   - beforeMount、mounted
   - beforeUpdate、updated
   - beforeDestroy、destroyed

   **父子组件生命周期执行顺序：**
   - 创建：父setup → 父onBeforeMount → 子setup → 子onBeforeMount → 子onMounted → 父onMounted
   - 更新：父onBeforeUpdate → 子onBeforeUpdate → 子onUpdated → 父onUpdated
   - 销毁：父onBeforeUnmount → 子onBeforeUnmount → 子onUnmounted → 父onUnmounted

2. **路由模式** → [Vue面试总结大全.md](../面试总结大全/Vue面试总结大全.md#五vue路由)

   **Hash模式：**
   - URL格式：`http://example.com/#/user`
   - 原理：监听hashchange事件
   - 优点：兼容性好、无需服务器配置
   - 缺点：URL不美观、SEO不友好

   **History模式：**
   - URL格式：`http://example.com/user`
   - 原理：HTML5 History API（pushState、replaceState）
   - 优点：URL美观、SEO友好
   - 缺点：需要服务器配置（404 fallback）

3. **路由守卫** → [Vue面试总结大全.md](../面试总结大全/Vue面试总结大全.md#3-路由守卫)

   **全局守卫：**
   ```javascript
   router.beforeEach((to, from, next) => {
     if (to.meta.requiresAuth && !isLoggedIn()) {
       next('/login');
     } else {
       next();
     }
   });

   router.afterEach((to, from) => {
     // 路由跳转后执行
   });
   ```

   **路由独享守卫：**
   ```javascript
   {
     path: '/admin',
     component: Admin,
     beforeEnter: (to, from, next) => {
       // 路由配置中定义
     }
   }
   ```

   **组件内守卫：**
   ```javascript
   export default {
     beforeRouteEnter(to, from, next) {},
     beforeRouteUpdate(to, from, next) {},
     beforeRouteLeave(to, from, next) {}
   }
   ```

4. **Vuex/Pinia状态管理** → [Vuex和Pinia面试题精简版.md](./Vuex和Pinia面试题精简版.md)

   **Vuex核心概念：**
   - State：状态数据
   - Getters：计算属性
   - Mutations：同步修改state
   - Actions：异步操作，提交mutation
   - Modules：模块化

   **Pinia优势：**
   - 更简洁的API，无需mutations
   - 完整的TypeScript支持
   - 自动代码分割
   - 更好的DevTools支持

   **Pinia基本使用：**
   ```javascript
   import { defineStore } from 'pinia';

   export const useUserStore = defineStore('user', {
     state: () => ({
       name: '',
       age: 0
     }),
     getters: {
       isAdult: (state) => state.age >= 18
     },
     actions: {
       updateName(name) {
         this.name = name;
       }
     }
   });
   ```

---

### 第五模块：性能优化进阶（0.5小时）→ [前端性能优化面试题精简版.md](./前端性能优化面试题精简版.md)

#### ✅ 必学知识点

1. **图片优化** → [前端性能优化面试题精简版.md](./前端性能优化面试题精简版.md#五图片优化)

   **图片格式选择：**
   - JPEG：照片、色彩丰富的图片
   - PNG：需要透明背景、logo
   - WebP：现代浏览器，体积更小
   - SVG：矢量图、图标

   **优化技巧：**
   - 图片压缩：TinyPNG、ImageOptim
   - 响应式图片：srcset、picture
   - 懒加载：IntersectionObserver
   - 雪碧图：减少HTTP请求
   - Base64：小图片内联

2. **懒加载** → [前端性能优化面试题精简版.md](./前端性能优化面试题精简版.md#五图片优化)

   **图片懒加载：**
   ```javascript
   const observer = new IntersectionObserver((entries) => {
     entries.forEach(entry => {
       if (entry.isIntersecting) {
         const img = entry.target;
         img.src = img.dataset.src;
         observer.unobserve(img);
       }
     });
   });

   document.querySelectorAll('img[data-src]').forEach(img => {
     observer.observe(img);
   });
   ```

   **路由懒加载：**
   ```javascript
   const Home = () => import('./views/Home.vue');
   const About = () => import('./views/About.vue');
   ```

3. **代码分割** → [前端性能优化面试题精简版.md](./前端性能优化面试题精简版.md#七webpackvite优化)

   **动态导入：**
   ```javascript
   // 按需加载
   button.addEventListener('click', async () => {
     const module = await import('./heavy-module.js');
     module.doSomething();
   });
   ```

   **Webpack配置：**
   ```javascript
   optimization: {
     splitChunks: {
       chunks: 'all',
       cacheGroups: {
         vendor: {
           test: /[\\/]node_modules[\\/]/,
           priority: -10
         }
       }
     }
   }
   ```

4. **Webpack优化** → [前端性能优化面试题精简版.md](./前端性能优化面试题精简版.md#七webpackvite优化)

   - Tree Shaking：删除未使用的代码
   - 代码压缩：TerserPlugin
   - 持久化缓存：contenthash
   - 并行构建：thread-loader
   - CDN加速：externals配置

---

## 📝 今日学习检验

### 必答问题清单（第2天）

#### HTML和CSS模块
1. ✅ 如何创建BFC？BFC有什么作用？
2. ✅ 层叠上下文的创建条件有哪些？
3. ✅ 列举3种水平垂直居中的方法？
4. ✅ 如何清除浮动？为什么要清除浮动？

#### JavaScript模块
5. ✅ 事件循环的执行顺序是什么？宏任务和微任务有哪些？
6. ✅ Promise的三种状态是什么？状态如何转换？
7. ✅ Promise.all和Promise.race的区别？
8. ✅ async/await如何处理错误？
9. ✅ ES Modules和CommonJS的区别？

#### HTTP网络模块
10. ✅ HTTP和HTTPS的区别？HTTPS如何加密？
11. ✅ GET和POST的区别？什么是幂等性？
12. ✅ 常见的HTTP状态码及其含义？
13. ✅ 强缓存和协商缓存的区别？缓存流程是什么？
14. ✅ 如何解决跨域问题？CORS的原理是什么？
15. ✅ XMLHttpRequest的五种状态是什么？

#### Vue模块
16. ✅ Vue3生命周期钩子有哪些？
17. ✅ 父子组件生命周期执行顺序是什么？
18. ✅ Hash模式和History模式的区别？
19. ✅ 路由守卫有哪些类型？如何使用？
20. ✅ Vuex和Pinia的区别？

#### 性能优化模块
21. ✅ 如何优化图片加载？图片格式如何选择？
22. ✅ 什么是懒加载？如何实现？
23. ✅ 什么是代码分割？如何实现？

---

## 🎯 明日预告

**第3天学习内容（5模块结构）：**
1. **HTML和CSS补充**：CSS3动画、移动端适配、预处理器
2. **JavaScript补充**：深拷贝浅拷贝、数组方法、手写代码
3. **Diff算法和迭代器专项**：虚拟DOM、Diff算法、Iterator
4. **Vue补充**：keep-alive、v-model原理、指令
5. **性能优化补充**：长列表优化、Vite优化、性能监控

---

**📌 学习提示：**
- 第2天内容较多，合理分配时间
- HTTP网络专项是重点，多做练习题
- 事件循环要结合代码例子理解
- 路由和状态管理要理解使用场景
- 每个模块学完后做相应的检验题
